// Locales

export { DEFAULT_LOCALE, LOCALES, ogLocale } from "./locales";

// Metadata

export {
  articleMetadata,
  blogPostMetadata,
  DEFAULT_DESCRIPTION,
  sectionMetadata,
  SITE_NAME,
} from "./metadata";

// URLs

export {
  canonicalUrl,
  hostUrl,
  languageAlternates,
  type SeoHost,
} from "./urls";
