export const TOTAL_CHUNKS = 20;

export const PERMS_PER_CHUNK = 60;

export const ELIMINATION_THRESHOLD = 0.08;

export const TOP_N_FRACTION = 0.15;

export const LOCK_THRESHOLD = 5;

export const STEP_LABELS_MAP: Record<string, string> = {
  configure: "Configure",
  import: "Import",
  inventory: "Inventory",
};
